
### Usage

1. Configure your routing in `routes.config.js` file.
1. Create related file into `routes` folder.
1. Edit the related file to return the response data.
1. Run `npm start` or `node -harmony app.js` to start the API proxy server.