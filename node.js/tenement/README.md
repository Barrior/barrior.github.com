### 目录结构
```
boostrap         # 启动初始化
config           # 配置
lib              # 工具库
middlewave       # 中间件
scripts          # 脚本
models           # m 数据层
routers          # c 控制层
views            # v 渲染层
public           # v-static 渲染层纯前端静态文件
test             # 测试
app.js           # 入口文件
```